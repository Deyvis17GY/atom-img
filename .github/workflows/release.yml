name: Release

on:
  push:
    branches: [master]
  workflow_dispatch:

# semantic-release needs to push the version commit, the tag and the release.
permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: pnpm/action-setup@v4

      # No `registry-url` here on purpose: it writes an .npmrc bound to
      # NODE_AUTH_TOKEN, which semantic-release then reads instead of writing its
      # own from NPM_TOKEN, and authentication fails on an unsubstituted placeholder.
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm test

      - run: pnpm exec semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
